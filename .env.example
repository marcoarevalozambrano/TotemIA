# Configuración de Base de Datos para TotemIA v2.0
# Copiar a .env y ajustar valores

# Motor: sqlite (default) o mariadb
DB_ENGINE=mariadb

# Configuración MariaDB (solo si DB_ENGINE=mariadb)
DB_NAME=totem_ia
DB_USER=totem
DB_PASSWORD=tu_password_aqui
DB_HOST=127.0.0.1
DB_PORT=3306
